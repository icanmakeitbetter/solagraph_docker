name: publish
on: [push]
jobs:
solargraph_docker-image:
runs-on: ubuntu-latest
steps:
- uses: actions/checkout@v2
- name: Login to GitHub Container Registry
uses: docker/login-action@v1
with:
registry: ghcr.io
username: ${{ github.actor }}
password: ${{ secrets.GITHUB_TOKEN }}
- name: Build the solargraph_docker Docker image
run: |
       docker build . --tag ghcr.io/icanmakeitbetter/solagraph_docker:latest
       docker run ghcr.io/icanmakeitbetter/solagraph_docker:latest
       docker push ghcr.io/icanmakeitbetter/solagraph_docker:latest
